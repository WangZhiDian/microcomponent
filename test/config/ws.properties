# tomcat 服务器会访问服务默认启动端口 server：port
# jetty 服务器，需要引入jetty对应的maven依赖，并设置特定端口

websocket.context-path=/websocket

system.context.switch.websocket=false
system.wsSessionTimeOutMinutes=30
system.maxWsSessionCount=10000
system.maxHttpResponseWaitSeconds=30

system.ws.msg.ids=
